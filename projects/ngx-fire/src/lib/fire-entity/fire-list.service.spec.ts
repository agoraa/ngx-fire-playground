import { TestBed } from '@angular/core/testing';
import { AngularFirestore } from '@angular/fire/firestore';
import { FireEntityService } from './fire-entity.service';
import { FireListService } from './fire-list.service';

describe('FireListService', () => {
  // beforeEach(() => TestBed.configureTestingModule({ providers: [AngularFirestore, FireEntityService, FireListService] }));
  //
  // it('should be created', () => {
  //   const service: FireListService<any> = TestBed.get(FireListService);
  //   expect(service).toBeTruthy();
  // });
});
